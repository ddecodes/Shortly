<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Shortly</h1>
    <% if (locals.id) { %>
        <p>Your short URL is: <a href="http://localhost:8001/url/<%= locals.id %>">http://localhost:8001/url/<%=
                    locals.id %></a></p>
        <% } %>
            <form action="/url" method="POST">
                <label>Enter your URL</label>
                <input type="text" name="url" placeholder="Enter your URL">
                <button type="submit">Shorten</button>
            </form>
            <% for (let i=0; i < locals.urls.length; i++) { %>
                <p>
                    <%= locals.urls[i].redirectUrl %>
                        <a href="http://localhost:8001/url/<%= locals.urls[i].shortId %>">http://localhost:8001/url/<%=
                                locals.urls[i].shortId %></a>
                </p>
                <% } %>

</body>

</html>